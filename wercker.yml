box: wercker/golang
build:
  steps:
    # golint step!
    # - wercker/golint

    # Build the project
    - script:
        name: go get
        code: |
          go get github.com/constabulary/gb/...

    # Test the project
    - script:
        name: go test
        code: |
          go test github.com/constabulary/gb/...
